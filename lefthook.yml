pre-commit:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
      fail_text: "TypeScript errors — fix before committing"
    lint:
      run: npm run lint
      fail_text: "ESLint errors — run npm run lint:fix or fix manually"
